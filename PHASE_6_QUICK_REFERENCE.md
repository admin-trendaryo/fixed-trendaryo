# Phase 6: Advanced Analytics & Reporting - Quick Reference Guide\n\n## 🚀 Quick Start\n\n### 1. Load Analytics Engine\n```html\n<script src=\"analytics-engine.js\"></script>\n```\n\n### 2. Access Global Instance\n```javascript\nwindow.AnalyticsEngine  // Automatically initialized\n```\n\n## 📊 Common Usage Patterns\n\n### Get Dashboard Metrics\n```javascript\n// Get all dashboard metrics for current month\nconst metrics = await window.AnalyticsEngine.getDashboardMetrics('month');\n\n// Access specific metrics\nconsole.log(metrics.sales.totalSales);\nconsole.log(metrics.customers.newCustomers);\nconsole.log(metrics.orders.avgOrderValue);\nconsole.log(metrics.revenue.totalRevenue);\n```\n\n### Get Customer Segmentation\n```javascript\nconst segments = await window.AnalyticsEngine.getCustomerSegmentation();\n\n// Access segments\nconsole.log(segments.segments.vip);      // VIP customers\nconsole.log(segments.segments.loyal);    // Loyal customers\nconsole.log(segments.segments.atrisk);   // At-risk customers\nconsole.log(segments.segments.inactive); // Inactive customers\nconsole.log(segments.segments.new);      // New customers\n```\n\n### Get Product Performance\n```javascript\nconst products = await window.AnalyticsEngine.getProductPerformance(10);\n\n// Top performers\nproducts.topPerformers.forEach(p => {\n  console.log(`${p.name}: ${p.unitsSold} units, ${p.revenue} revenue`);\n});\n\n// Underperformers\nproducts.underperformers.forEach(p => {\n  console.log(`${p.name}: Needs attention`);\n});\n```\n\n### Get Conversion Funnel\n```javascript\nconst funnel = await window.AnalyticsEngine.getConversionFunnel();\n\n// Funnel stages\nconsole.log(`Visitors: ${funnel.stages.visitors}`);\nconsole.log(`Added to Cart: ${funnel.stages.addedToCart}`);\nconsole.log(`Checkout: ${funnel.stages.initiatedCheckout}`);\nconsole.log(`Completed: ${funnel.stages.completed}`);\n\n// Conversion rates\nconsole.log(`Overall Conversion: ${funnel.conversionRates.overall}%`);\n```\n\n### Forecast Sales\n```javascript\nconst forecast = await window.AnalyticsEngine.forecastSales(12);\n\nconsole.log(`Trend: ${forecast.trend}`);\nconsole.log(`Confidence: ${forecast.confidence * 100}%`);\nconsole.log(`Forecast Data:`, forecast.forecast);\n```\n\n### Predict Churn\n```javascript\nconst churn = await window.AnalyticsEngine.predictChurn();\n\n// At-risk customers\nchurn.atRiskCustomers.forEach(customer => {\n  console.log(`${customer.name}: ${customer.churnProbability}% risk`);\n});\n\n// Recommendations\nchurn.recommendations.forEach(rec => {\n  console.log(`${rec.title}: ${rec.description}`);\n});\n```\n\n### Predict Demand\n```javascript\nconst demand = await window.AnalyticsEngine.predictDemand();\n\ndemand.predictions.forEach(pred => {\n  console.log(`${pred.productName}: ${pred.predictedDemand} units`);\n  console.log(`Recommended Stock: ${pred.recommendedStock}`);\n});\n```\n\n### Predict Customer LTV\n```javascript\nconst clv = await window.AnalyticsEngine.predictCLV();\n\nclv.predictions.forEach(pred => {\n  console.log(`${pred.customerName}: ${pred.predictedLTV} LTV`);\n  console.log(`Growth Potential: ${pred.growthPotential}%`);\n});\n```\n\n### Generate Custom Report\n```javascript\nconst config = {\n  title: 'Monthly Sales Report',\n  type: 'sales',\n  startDate: '2024-01-01',\n  endDate: '2024-01-31',\n  metrics: ['revenue', 'orders', 'aov', 'conversion'],\n  filters: 'category=electronics'\n};\n\nconst report = await window.AnalyticsEngine.generateCustomReport(config);\nconsole.log(`Report ID: ${report.reportId}`);\nconsole.log(`Data Records: ${report.data.length}`);\n```\n\n### Export Report\n```javascript\n// Export to PDF\nawait window.AnalyticsEngine.exportToPDF(reportId, 'sales-report');\n\n// Export to Excel\nawait window.AnalyticsEngine.exportToExcel(reportId, 'sales-report');\n\n// Email report\nawait window.AnalyticsEngine.emailReport(\n  reportId,\n  ['admin@example.com'],\n  'Monthly Sales Report'\n);\n```\n\n### Schedule Report\n```javascript\nconst config = {\n  templateId: 'sales-report',\n  frequency: 'weekly',\n  recipients: ['admin@example.com'],\n  time: '09:00'\n};\n\nconst schedule = await window.AnalyticsEngine.scheduleReport(config);\nconsole.log(`Schedule ID: ${schedule.scheduleId}`);\nconsole.log(`Next Run: ${schedule.nextRun}`);\n```\n\n### Set KPI Alert\n```javascript\nconst alert = await window.AnalyticsEngine.setKPIAlert(\n  'revenue',\n  10000,\n  'below'\n);\n\nconsole.log('Alert set for revenue below $10,000');\n```\n\n### Get Active Alerts\n```javascript\nconst alerts = await window.AnalyticsEngine.getActiveAlerts();\n\nalerts.forEach(alert => {\n  console.log(`${alert.title}: ${alert.message}`);\n});\n```\n\n## 🎨 UI Integration Examples\n\n### Display Metrics in HTML\n```html\n<div class=\"metric-card\">\n  <div class=\"metric-label\">Total Revenue</div>\n  <div class=\"metric-value\" id=\"totalRevenue\"></div>\n  <div class=\"metric-change\" id=\"revenueChange\"></div>\n</div>\n\n<script>\n  async function displayMetrics() {\n    const metrics = await window.AnalyticsEngine.getDashboardMetrics('month');\n    document.getElementById('totalRevenue').textContent = \n      window.AnalyticsEngine.formatCurrency(metrics.revenue.totalRevenue);\n    document.getElementById('revenueChange').textContent = \n      `${metrics.revenue.growth >= 0 ? '+' : ''}${metrics.revenue.growth}%`;\n  }\n  displayMetrics();\n</script>\n```\n\n### Display Segmentation\n```html\n<div class=\"segment-grid\" id=\"segmentGrid\"></div>\n\n<script>\n  async function displaySegments() {\n    const data = await window.AnalyticsEngine.getCustomerSegmentation();\n    const html = Object.entries(data.segments).map(([name, segment]) => `\n      <div class=\"segment-card\">\n        <div class=\"segment-name\">${name}</div>\n        <div class=\"segment-count\">${segment.count}</div>\n        <div class=\"segment-revenue\">${window.AnalyticsEngine.formatCurrency(segment.revenue)}</div>\n      </div>\n    `).join('');\n    document.getElementById('segmentGrid').innerHTML = html;\n  }\n  displaySegments();\n</script>\n```\n\n### Display Funnel\n```html\n<div id=\"funnelContainer\"></div>\n\n<script>\n  async function displayFunnel() {\n    const funnel = await window.AnalyticsEngine.getConversionFunnel();\n    const stages = funnel.stages;\n    const rates = funnel.conversionRates;\n    \n    const html = `\n      <div class=\"funnel-stage\">\n        <div class=\"funnel-bar\" style=\"width: 100%;\">Visitors: ${stages.visitors}</div>\n        <div class=\"funnel-rate\">100%</div>\n      </div>\n      <div class=\"funnel-stage\">\n        <div class=\"funnel-bar\" style=\"width: ${rates.visitorToCart}%;\">Cart: ${stages.addedToCart}</div>\n        <div class=\"funnel-rate\">${rates.visitorToCart}%</div>\n      </div>\n      <div class=\"funnel-stage\">\n        <div class=\"funnel-bar\" style=\"width: ${rates.cartToCheckout}%;\">Checkout: ${stages.initiatedCheckout}</div>\n        <div class=\"funnel-rate\">${rates.cartToCheckout}%</div>\n      </div>\n      <div class=\"funnel-stage\">\n        <div class=\"funnel-bar\" style=\"width: ${rates.checkoutToComplete}%;\">Completed: ${stages.completed}</div>\n        <div class=\"funnel-rate\">${rates.checkoutToComplete}%</div>\n      </div>\n    `;\n    document.getElementById('funnelContainer').innerHTML = html;\n  }\n  displayFunnel();\n</script>\n```\n\n## 🔧 Utility Functions\n\n### Format Currency\n```javascript\nconst formatted = window.AnalyticsEngine.formatCurrency(1234.56, 'USD');\n// Output: $1,234.56\n```\n\n### Format Percentage\n```javascript\nconst formatted = window.AnalyticsEngine.formatPercentage(45.678);\n// Output: 45.68%\n```\n\n### Calculate Growth\n```javascript\nconst growth = window.AnalyticsEngine.calculateGrowth(1000, 800);\n// Output: 25.00\n```\n\n### Calculate Rate\n```javascript\nconst rate = window.AnalyticsEngine.calculateRate(50, 200);\n// Output: 25.00\n```\n\n### Clear Cache\n```javascript\nwindow.AnalyticsEngine.clearCache();\n```\n\n## 📱 Responsive Design Classes\n\n### Mobile Breakpoints\n```css\n/* Mobile: < 768px */\n@media (max-width: 768px) {\n  .kpi-grid {\n    grid-template-columns: 1fr;\n  }\n}\n\n/* Tablet: 768px - 1024px */\n@media (min-width: 768px) and (max-width: 1024px) {\n  .kpi-grid {\n    grid-template-columns: repeat(2, 1fr);\n  }\n}\n\n/* Desktop: > 1024px */\n@media (min-width: 1024px) {\n  .kpi-grid {\n    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));\n  }\n}\n```\n\n## 🔐 Security Best Practices\n\n### Validate Admin Access\n```javascript\nif (!window.AdminManager.isAdmin()) {\n  alert('Access denied');\n  window.location.href = 'admin-login.html';\n}\n```\n\n### Check Token Validity\n```javascript\nconst token = window.AdminManager.getAdminToken();\nif (!token) {\n  // Redirect to login\n  window.location.href = 'admin-login.html';\n}\n```\n\n### Log Admin Actions\n```javascript\nawait window.AdminManager.logAdminAction('report_generated', {\n  reportId: reportId,\n  reportType: 'sales',\n  timestamp: new Date()\n});\n```\n\n## 🐛 Error Handling\n\n### Try-Catch Pattern\n```javascript\ntry {\n  const metrics = await window.AnalyticsEngine.getDashboardMetrics('month');\n  displayMetrics(metrics);\n} catch (error) {\n  console.error('Error loading metrics:', error);\n  showErrorMessage('Failed to load analytics data');\n}\n```\n\n### Error Messages\n```javascript\nfunction showErrorMessage(message) {\n  const errorDiv = document.createElement('div');\n  errorDiv.className = 'error-message';\n  errorDiv.textContent = message;\n  document.body.appendChild(errorDiv);\n  setTimeout(() => errorDiv.remove(), 5000);\n}\n```\n\n## 📈 Performance Tips\n\n### Use Caching\n```javascript\n// Cache is automatically managed (5-minute expiry)\n// Manual cache clear when needed\nwindow.AnalyticsEngine.clearCache();\n```\n\n### Batch Requests\n```javascript\n// Load multiple metrics in parallel\nconst [sales, customers, orders] = await Promise.all([\n  window.AnalyticsEngine.getSalesMetrics('month'),\n  window.AnalyticsEngine.getCustomerMetrics('month'),\n  window.AnalyticsEngine.getOrderMetrics('month')\n]);\n```\n\n### Lazy Load Data\n```javascript\n// Load data only when tab is clicked\nfunction switchTab(tabName) {\n  if (tabName === 'products') {\n    loadProductPerformance();\n  }\n}\n```\n\n## 🎯 Common Workflows\n\n### Daily Dashboard Check\n```javascript\nasync function dailyDashboardCheck() {\n  const metrics = await window.AnalyticsEngine.getDashboardMetrics('day');\n  const alerts = await window.AnalyticsEngine.getActiveAlerts();\n  \n  console.log('Daily Metrics:', metrics);\n  console.log('Active Alerts:', alerts);\n}\n```\n\n### Weekly Report Generation\n```javascript\nasync function generateWeeklyReport() {\n  const config = {\n    title: 'Weekly Performance Report',\n    type: 'custom',\n    startDate: getLastMonday(),\n    endDate: new Date().toISOString().split('T')[0],\n    metrics: ['revenue', 'orders', 'customers', 'conversion']\n  };\n  \n  const report = await window.AnalyticsEngine.generateCustomReport(config);\n  await window.AnalyticsEngine.emailReport(\n    report.reportId,\n    ['team@example.com'],\n    'Weekly Performance Report'\n  );\n}\n```\n\n### Monthly Forecast Review\n```javascript\nasync function monthlyForecastReview() {\n  const forecast = await window.AnalyticsEngine.forecastSales(12);\n  const churn = await window.AnalyticsEngine.predictChurn();\n  const demand = await window.AnalyticsEngine.predictDemand();\n  \n  console.log('Sales Forecast:', forecast);\n  console.log('Churn Predictions:', churn);\n  console.log('Demand Forecast:', demand);\n}\n```\n\n## 📚 Additional Resources\n\n- **Implementation Guide**: PHASE_6_IMPLEMENTATION_GUIDE.md\n- **Completion Summary**: PHASE_6_COMPLETE.txt\n- **Admin Manager**: admin-manager.js\n- **API Documentation**: Backend API docs\n\n---\n\n**Last Updated**: Phase 6 Complete\n**Version**: 1.0\n**Status**: Production Ready\n